<%= simple_form_for([@cocktail, @dose]) do |f| %>
<%= f.association :ingredient %>
<%= f.label :description, label: "Enter a measurement of ingredient for your cocktail here" %>
<%= f.text_field :description %>
<%= f.submit class: "btn btn-primary"%>
<% end %>
